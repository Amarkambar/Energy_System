# requirements-vercel.txt — slim deps for Vercel serverless deployment
# Heavy optional packages (prophet, optuna, statsmodels, plotly, matplotlib,
# seaborn, reportlab, redis, watchdog, openpyxl) are excluded to stay under
# Vercel's 250 MB compressed function size limit.

# ── Core API ──────────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn>=0.29.0
mangum>=0.17.0
pydantic[email]>=2.6.0
pydantic-settings>=2.2.0
python-multipart>=0.0.9
starlette>=0.37.0

# ── Database ──────────────────────────────────────────────────────────────────
pymongo>=4.6.0

# ── ML & Data (trimmed — only what the pipeline actually needs) ───────────────
pandas>=2.1.0
numpy>=1.24.3
scikit-learn>=1.3.0
xgboost>=1.7.6
shap>=0.42.1
scipy>=1.11.2
pyarrow>=13.0.0

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
requests>=2.31.0

# ── Security ──────────────────────────────────────────────────────────────────
passlib[bcrypt]>=1.7.4
bcrypt==4.0.1
